# Exemplo de .env para o backend Node de reCAPTCHA
# Copie para .env e preencha os valores reais

# Chave secreta do reCAPTCHA (do painel do Google, não a site key)
RECAPTCHA_SECRET_KEY=YOUR_RECAPTCHA_SECRET_KEY

# Porta do servidor (opcional)
PORT=4000
